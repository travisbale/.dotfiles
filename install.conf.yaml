- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: .gitconfig
    ~/.zshrc: .zshrc
    ~/.tmux.conf: .tmux.conf
    ~/.config/Code/User/settings.json: vscode/settings.json
    ~/.vimrc: .vimrc
    ~/.config/nvim/init.vim: nvim/init.vim
